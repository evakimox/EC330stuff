#include "Problem3a.h"
#include <vector>

using namespace std;

int main(){
  //The list I am implementing is vector
  vector<omniCell<int> > nums;
  vector<omniCell<string> > strings;
  omniCell<int> num1(12);
  omniCell<int> num2(22);
  nums.push_back(num1);
  

  return 0;
}
